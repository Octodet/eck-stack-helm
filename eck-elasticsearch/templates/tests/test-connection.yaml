apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eck-elasticsearch.fullname" . }}-test-connection"
  labels:
    {{- include "eck-elasticsearch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eck-elasticsearch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
