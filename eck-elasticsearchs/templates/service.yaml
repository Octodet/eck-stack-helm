apiVersion: v1
kind: Service
metadata:
  name: {{ include "eck-elasticsearchs.fullname" . }}
  labels:
    {{- include "eck-elasticsearchs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eck-elasticsearchs.selectorLabels" . | nindent 4 }}
